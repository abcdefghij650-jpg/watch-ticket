name: Ticket Watcher

on:
  schedule:
    - cron: "*/1 * * * *"  # 1分ごと
  workflow_dispatch:

jobs:
  watch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run watcher
        run: python watch.py
